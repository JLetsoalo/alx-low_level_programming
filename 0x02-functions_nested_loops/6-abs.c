#include "main.h"
/**
 *_abs - calculates absolute value
 *@n: integer agrument
 *Return: returns absolute value
 */

int _abs(int)
{
if (int > 0)
return (int);
else if (int < 0)
return (-int);

return (0);
}
