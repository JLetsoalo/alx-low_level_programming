#include "main.h"
/**
 *_abs - calculates absolute value
 *@n: integer agrument
 *Return: returns absolute value
 */

int _abs(int)
{
if (n > 0)
return (n);
else if (n < 0)
return (-n);

return (0)
}
