#include "main.h"

/**
 * _abs - calculates absolute value of an integer
 * @n: value to comoute
 * Return: absolute value of computed n
 */

int _abs(int n)
{
	if (n >= 0)
	{
		return (n);
	}

	return (-n);
}
